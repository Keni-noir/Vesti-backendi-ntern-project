# Use postgres/example user/password credentialss

services:

  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: QPag#@2003

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
